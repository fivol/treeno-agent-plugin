{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Treeno memory plugin: the treeno MCP server is your long-term memory about this user. Call its memory_context tool once now (before your first answer) and follow the memory policy it returns: recall with memory_search, save only what will matter later with memory_write. If the treeno tools are missing or need authentication, ask the user to connect Treeno (in Claude Code: run /mcp) and choose AI memory folder only when signing in.'"
          }
        ]
      }
    ]
  }
}
